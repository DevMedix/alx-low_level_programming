#include "main.h"
/**
 * add - adds two integer numbers a and b
 * @a: number to be added to b
 * @b: number to be added to a
 *
 * Return: returns the result of the added numbers
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
